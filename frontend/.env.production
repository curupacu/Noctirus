# Usado só no build de produção (npm run build). Sobrescreve VITE_API_URL do .env
# (que aponta pro backend local) — o site no ar precisa falar com o backend real.
VITE_API_URL=https://noctirus-backend.onrender.com
